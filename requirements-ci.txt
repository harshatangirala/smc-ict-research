# CI dependency set: the research pipeline plus test tooling, without the
# dashboard/PDF stack (gradio, streamlit, playwright), which is not exercised
# by CI and dominates install time.
pandas>=2.1
numpy>=1.26
scipy>=1.11
pyarrow>=14.0
statsmodels>=0.14
openpyxl>=3.1
joblib>=1.3
tqdm>=4.66
python-dateutil>=2.8
matplotlib>=3.8
pytest>=7.4
pytest-cov>=4.1
