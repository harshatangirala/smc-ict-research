# Core data & numerics
pandas>=2.1
numpy>=1.26
scipy>=1.11
pyarrow>=14.0

# Market data acquisition
yfinance>=0.2.40

# Statistics / research
statsmodels>=0.14

# Excel / spreadsheet I/O
openpyxl>=3.1

# Parallelization utilities
joblib>=1.3

# Progress bars / CLI ergonomics
tqdm>=4.66

# Visualization
plotly>=5.20
matplotlib>=3.8          # manuscript figures (tools/make_figures.py)

# Dashboard
gradio>=4.40
streamlit>=1.50

# Manuscript rendering
# tools/build_manuscript_pdf.py needs only `markdown` plus a Chrome/Chromium/Edge
# binary, which is already present on most machines. playwright is optional and
# used by the older utils/render_pdf.py path.
markdown>=3.5
pypdf>=4.3
playwright>=1.47

# Testing
pytest>=7.4
pytest-cov>=4.1

# Config / typing niceties
pydantic>=2.6
python-dateutil>=2.8
