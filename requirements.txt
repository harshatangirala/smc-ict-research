# Core data & numerics
pandas>=2.1,<4
numpy>=1.26,<3
scipy>=1.11,<2
pyarrow>=14.0,<25

# Market data acquisition
yfinance>=0.2.40,<2

# Statistics / research
statsmodels>=0.14,<1

# Excel / spreadsheet I/O
openpyxl>=3.1,<4

# Parallelization utilities
joblib>=1.3,<2

# Progress bars / CLI ergonomics
tqdm>=4.66,<5

# Visualization
plotly>=5.20,<7

# Dashboard
# gradio pinned to the 4.x line the code was written against (see README's
# Hugging Face Spaces sdk_version: 4.44.0) -- Gradio 6 moved theme/css from
# the Blocks constructor to launch(), breaking app.py's current call.
gradio>=4.40,<5
streamlit>=1.50,<2

# PDF report rendering (utils/render_pdf.py)
playwright>=1.47,<2
pypdf>=4.3,<7

# Testing
pytest>=7.4,<10
pytest-cov>=4.1,<8

# Config / typing niceties
pydantic>=2.6,<3
python-dateutil>=2.8,<3
