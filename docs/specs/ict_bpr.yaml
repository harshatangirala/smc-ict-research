# Formal specification -- ICT Balanced Price Range
# Auto-generated by tools/generate_specs.py; edit that file, not this one.
# Schema: docs/specs/_schema.yaml

concept: "ICT Balanced Price Range"
id: ict_bpr
source_script: "ICT_Concepts_LuxAlgo.pine"
source_section: "1.7"
implementation: "signals/ict_signals.py::detect_bpr"

signals:
  - name: ict_bpr_bullish
    direction: +1
  - name: ict_bpr_bearish
    direction: -1

definition: |
  A = most recent bullish FVG region [a_lo, a_hi]
  B = most recent bearish FVG region [b_lo, b_hi]
  overlap_i = max(a_lo, b_lo) < min(a_hi, b_hi)
  bullish_i = overlap_i AND fvg_bullish_formed_i
  bearish_i = overlap_i AND fvg_bearish_formed_i

parameters:
  []

causality: |
  Both regions are carried forward from bars <= i.

expected_min_occurrences: 50

notes: |
  DEFECT FIXED: the prior condition `(up_bot < dn_top) AND (dn_bot <
  up_bot)` was written against inverted column names and reduces to
  `dn_upper < dn_lower`, which is unsatisfiable. Both signals were
  identically False for every ticker and bar, and melt_events drops all-
  False columns silently, so the concept vanished from the study with no
  error.
